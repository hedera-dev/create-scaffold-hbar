---
"create-scaffold-hbar": minor
---

Quiet yarn/skills/`forge install` output under Listr status, structured template outro (`sections`, `{run:framework:…}`), and an offline template registry to avoid GitHub rate limits. Prefer `outro.sections` (`outro.steps` remains accepted but deprecated). Default outro frontend command is `next:dev`.
